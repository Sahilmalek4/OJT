#include<stdio.h>
     // this is the porgram for  find the area of circle if we know the redius of the circle  
 float main()
 {
    float radius,area,pi;
    pi=3.14;
    printf("Enter the radius of circle:");
    scanf("%f",&radius);
    area = radius*radius*pi;
    printf("The area of a circle is :%f",area);
    printf("squre unit");
      return 0;
    }
